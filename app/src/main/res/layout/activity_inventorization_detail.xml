<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootFrame"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- Вложенный LinearLayout — твой исходный UI, без изменений цвета/структуры -->
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:background="@color/primary"
        android:layout_height="match_parent">

        <!-- Верхняя панель -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="8dp"
            android:paddingEnd="8dp">

            <!-- Стрелка назад -->
            <ImageView
                android:id="@+id/backArrow"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="7dp"
                android:src="@drawable/ic_back"
                android:contentDescription="Back"
                android:tint="@color/white"/>

            <!-- Пустой View -->
            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <!-- Счетчик -->
            <TextView
                android:id="@+id/tagCounter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0(0)"
                android:textSize="24sp"
                android:textFontWeight="700"
                android:layout_marginEnd="0dp"
                android:textColor="@color/white" />

            <!-- Иконка сканера -->
            <ImageView
                android:id="@+id/scanIcon"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:src="@drawable/ic_scanner"
                android:contentDescription="Scanner Icon"
                android:tint="@color/red"/>
        </LinearLayout>

        <!-- Список занимает всё доступное пространство -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/detailRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white"/>
    </LinearLayout>

    <!-- Overlay: иконка статуса поверх списка, по центру внизу -->
    <FrameLayout
        android:id="@+id/statusIconContainer"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="16dp">

        <ImageView
            android:id="@+id/statusIcon"
            android:layout_width="65dp"
            android:layout_height="65dp"
            android:layout_gravity="center"
            android:src="@drawable/finish"
            android:contentDescription="Status Icon" />
    </FrameLayout>

</FrameLayout>